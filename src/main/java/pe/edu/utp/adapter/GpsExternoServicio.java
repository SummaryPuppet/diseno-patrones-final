package pe.edu.utp.adapter;

public class GpsExternoServicio {
    public String obtenerCoordenadas(String deviceId) {
        // Simulación de llamada a un servicio externo de GPS
        // En un caso real, aquí se haría una llamada HTTP o similar
        return "Latitud: -12.0464, Longitud: -77.0428"; // Coordenadas de ejemplo (Lima, Perú)
    }
}
